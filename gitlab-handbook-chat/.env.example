# Use localhost when Docker Compose and SecretChat.exe run on the same computer.
# For another computer in the LAN, use the server hostname/IP covered by its TLS certificate.
GATEWAY_HOST=localhost
GATEWAY_PORT=8088
GATEWAY_SCHEME=https
